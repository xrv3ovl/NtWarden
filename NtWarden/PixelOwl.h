#pragma once

#include "imgui.h"

// Animated pixel owl overlay for the top-right corner.
// The owl perches, blinks, bobs slightly, and occasionally tracks the cursor.

namespace PixelOwl {
    void Render();
}
